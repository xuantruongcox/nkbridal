import { QueryList, ElementRef, AfterViewInit } from '@angular/core';
import { CarouselItemDirective } from './carousel-item.directive';
export declare class CarouselComponent implements AfterViewInit {
    carouselInners: QueryList<CarouselItemDirective>;
    carouselItems: QueryList<ElementRef<HTMLElement>>;
    /** Enable autoChange of carousel items */
    autochange: boolean;
    /** Enable transit animation */
    animationType: 'none' | 'fade';
    /** Period for autoChange */
    period: number;
    private _activeIndex;
    private _subscriber;
    private _isAnimation;
    ngAfterViewInit(): void;
    private setupCarousel;
    selectAt(indexPath: number): void;
    next(): void;
    prev(): void;
    private setItemActive;
    private setItemInactive;
}
